extends layout

block content
  h1= title
  //- let Bath_essentials = ['Bath_tub_Type','Bath_Towel_Type','Cost']
  - let Bath_essentials = results;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each results, index in Bath_essentials
        div(class='col-sm-2' onclick='buy('+index+')') #{results}